## YAML Template.
template: Hello, %s!
defaultName: Stranger

server:
  applicationConnectors:
  - type: http 
    port: 9000
  adminConnectors:
  - type: http
    port: 9001
  requestLog:
    timeZone: UTC
    
logging:
  appenders:
    - type: console
      threshold: TRACE
      target: stderr